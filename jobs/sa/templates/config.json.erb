
{
    "SA": {
        "GRPC": {
            "address": "xx",
            "clientNames": ["foo", "bar", "baz"],
        },
        "TLS": {
            "CertFile": "/",
            "KeyFile": "/",
            "CACertFile": "/",
        },
        "DBConnect": "<%= p("db.username") %>:<%= p("db.password") %>@tcp(<%= p("db.host") %>:<%= p("db.port") %>)/<%= p("db.database") %>",
        "Features": {},
        "ParallelismPerRPC": 10,
    },
    "Syslog": {
    }
}
